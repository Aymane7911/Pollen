# Copy to .env (`cp .env.example .env`). SQLite needs no server.
# Swap to PostgreSQL by changing the datasource provider in prisma/schema.prisma
# and this URL to e.g. postgresql://user:pass@localhost:5432/atlas
DATABASE_URL="file:./dev.db"
